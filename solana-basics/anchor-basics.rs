// ANCHOR

// anchor generates Interface Description Language (IDL) files, which describle the program's structure & functionality + enables creation of JS/TS client libraries.
// uses parallel execution
// solana uses rent


// set default solana configuration:
// solana config get

// change configuration to use local cluster:
// solana config set --url localhost


